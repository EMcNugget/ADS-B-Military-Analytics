{
  "name": "adsb-mil-data",
  "$schema": "node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "./src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/adsb-mil-data"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "serve": {
      "executor": "@nrwl/vite:dev-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "adsb-mil-data:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "adsb-mil-data:build:development",
          "hmr": true
        },
        "production": {
          "buildTarget": "adsb-mil-data:build:production",
          "hmr": false
        }
      }
    },
    "preview": {
      "executor": "@nrwl/vite:preview-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "adsb-mil-data:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "adsb-mil-data:build:development"
        },
        "production": {
          "buildTarget": "adsb-mil-data:build:production"
        }
      }
    },
    "test": {
      "executor": "@nrwl/vite:test",
      "outputs": ["coverage/adsb-mil-data"],
      "options": {
        "passWithNoTests": true,
        "reportsDirectory": "coverage/adsb-mil-data"
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["./src/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
