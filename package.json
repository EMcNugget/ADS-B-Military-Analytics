{
  "name": "adsb_mil_data",
  "version": "1.0.0",
  "description": "Analytics for military aircraft over ADSB.",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "python3 app.py && cd server && python3 main.py && cd .. && cd client && npm run dev && cd ..",
    "install": "sh ./build.sh"
  }
}
