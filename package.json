{
  "name": "adsb_mil_data",
  "version": "1.0.0",
  "description": "Analytics for military aircraft over ADSB.",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "dev": "cd client && npm run dev",
    "start": "cd cli && cd linux && ./run.sh",
    "init": "cd cli && cd linux && ./build.sh",
    "winrun": "cli\\windows\\run.bat",
    "wininit": "cli\\windows\\build.bat"
  }
}
